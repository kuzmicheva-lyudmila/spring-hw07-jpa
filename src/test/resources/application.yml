spring:
 datasource:
  url: jdbc:h2:mem:testdb
  #schema: test-app-schema.sql
  data: test-data.sql
 jpa:
  generate-ddl: true
  hibernate:
   ddl-auto: create
  show-sql: true
 flyway:
  enabled: false

logging:
 level:
  ROOT: DEBUG
